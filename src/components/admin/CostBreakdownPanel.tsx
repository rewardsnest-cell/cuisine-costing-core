// ARCHIVED — Pricing v1 cost breakdown panel. See /docs/pricing-archive.md.
export function CostBreakdownPanel(_props: { referenceId: string }) {
  return (
    <div className="text-xs text-muted-foreground italic">
      Cost breakdown is archived (Pricing v1). Pricing v2 will replace this.
    </div>
  );
}
export default CostBreakdownPanel;
