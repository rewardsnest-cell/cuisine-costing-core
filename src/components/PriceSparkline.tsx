// ARCHIVED — Pricing v1 PriceSparkline. The price_history table moved to the
// archive schema and is no longer queried at runtime. Renders nothing.
// See /docs/pricing-archive.md.
export function PriceSparkline(_props: { inventoryItemId?: string; className?: string }) {
  return null;
}
